# .abc.config sets sim_backend=xsim, but an explicit --sim-backend vivado
# on the command line must win and run the full Vivado project flow.
ABC_XSIM_CALL=(--vivado-version 2024.1 --sim-backend vivado -sim "$REPO/tb/top.abc")
EXPECT_EXIT_CODE=0
